[project]
name = "ai-spending-tracker-backend"
version = "0.1.0"
description = "FastAPI backend for the AI Spending Tracker"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
]

[tool.uv]
package = false
